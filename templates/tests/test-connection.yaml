apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-app-hart.fullname" . }}-test-connection"
  labels:
    {{- include "devops-app-hart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-app-hart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
