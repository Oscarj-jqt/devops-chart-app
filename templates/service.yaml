apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-app-hart.fullname" . }}
  labels:
    {{- include "devops-app-hart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-app-hart.selectorLabels" . | nindent 4 }}
